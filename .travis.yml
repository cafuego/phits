language: php

php:
  - 5.4
  - 5.5

before_script:
- composer install --prefer-source --dev
- composer require squizlabs/php_codesniffer:1.5.3

script:
- vendor/bin/phpcs --standard=PSR1 --extensions=php --ignore=vendor/* .
- vendor/bin/phpunit ./tests/Phits/Fits/TestFits
